#include <stdio.h>
void main()
{
    int n, m;

    printf("Escreva um valor numerico inteiro positivo ou negativo: ");
    scanf("%d", &n);

    if(n>0)
    {
        printf("%d",n);
    }

    else
    {
        m = n*(-1);
        printf("%d",m);
    }
}
